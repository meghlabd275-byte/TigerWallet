[package]
name = "tigerswap-bridge-engine"
version = "1.0.0"
edition = "2021"
description = "Cross-chain bridge engine for TigerSwap"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
parking_lot = "0.12"

[lib]
path = "src/lib.rs"
