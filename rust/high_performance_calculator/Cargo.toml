[package]
name = "high_performance_calculator"
version = "0.1.0"
edition = "2021"

[dependencies]
parking_lot = "0.12"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
